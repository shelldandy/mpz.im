---
import { getEntry } from 'astro:content';
import PageError from '../layouts/PageError.astro';


const errorPage = await getEntry('pages', '404');
const { Content } = await errorPage.render();
---

<PageError>
  <Content />
</PageError>
